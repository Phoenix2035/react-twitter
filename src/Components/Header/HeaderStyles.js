import {makeStyles} from "@material-ui/styles";

const useStyle = makeStyles(theme => ({
    header: {
        padding: 18,
        backgroundColor: '#fff',
        display: 'flex'
    },
    headerTitle: {
        fontSize: '1.1rem',
        fontWeight: '600',
        marginRight: '.5rem'
    },


}))

export default useStyle